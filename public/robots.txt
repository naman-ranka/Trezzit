# https://www.robotstxt.org/robotstxt.html
#
# The marketing site is fully crawlable. /s/ and /demo only redirect
# (to the app's shared-bill pages, which are private by design), so
# there is nothing there for a crawler to fetch.

User-agent: *
Allow: /
Disallow: /s/
Disallow: /demo

Sitemap: https://trezzit.com/sitemap.xml
